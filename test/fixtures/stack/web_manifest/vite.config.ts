// M-STACK.1 fixture — config-file EVIDENCE for a frontend the IR cannot
// parse (M-LANG3 registers .ts/.tsx only, and this project's pages are
// .jsx). The stack index must report these facts labelled `config`.
export default { root: "." };
